{
  "firestore": {
    "collections": [
      {
        "name": "users",
        "description": "Stores user profile information.",
        "schema": {
          "uid": { "type": "string", "description": "Unique identifier for the user, typically from Firebase Authentication." },
          "email": { "type": "string", "description": "User's email address." },
          "displayName": { "type": "string", "description": "User's display name." },
          "photoURL": { "type": "string", "description": "URL of the user's profile picture." },
          "createdAt": { "type": "timestamp", "description": "Timestamp of when the user account was created." }
        }
      },
      {
        "name": "profiles",
        "description": "Represents a user profile within the vaselearn application.",
        "schema": {
          "clientType": { "type": "string", "description": "The type of client, e.g., 'Adult Education Center'." },
          "hasOnboarded": { "type": "boolean", "description": "Flag indicating if the user has completed the onboarding process." }
        }
      },
      {
        "name": "notes",
        "description": "Represents a user's note within the vaselearn application.",
        "schema": {
          "content": { "type": "string", "description": "The content of the note." },
          "userId": { "type": "string", "description": "The ID of the user who created the note." },
          "createdAt": { "type": "timestamp", "description": "Timestamp of when the note was created." }
        }
      }
    ]
  }
}
